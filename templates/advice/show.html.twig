{% extends 'base.html.twig' %}

{% block title %}Conseil{% endblock %}

{% block body %}
    <h1>Conseil</h1>

    <table class="table table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ advice.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ advice.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ advice.Content }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ advice.Slug }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ advice.status }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ advice.createdAt ? advice.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ advice.updatedAt ? advice.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>
    
    <div class="d-flex justify-content-between">
    <a href="{{ path('app_backoffice_advices_list') }}" class="btn btn-outline-secondary">Retour à la liste</a>
    <a href="{{ path('app_backoffice_advices_edit', {'id': advice.id}) }}" class="btn btn-success mx-auto">Valider</a>
    </div>

    {{ include('advice/_delete_form.html.twig') }}
{% endblock %}
