{% extends 'base.html.twig' %}

{% block title %}Les conseils{% endblock %}

{% block body %}
    <h1>Les conseils</h1>

    <table class="table table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ advice.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ advice.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ advice.Content }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ advice.Slug }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ advice.status }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ advice.createdAt ? advice.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Updated_at</th>
                <td>{{ advice.updatedAt ? advice.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_backoffice_advices_list') }}">Retour à la liste</a>

    <a href="{{ path('app_backoffice_advices_edit', {'id': advice.id}) }}">Ajouter ce conseil</a>

    {{ include('advice/_delete_form.html.twig') }}
{% endblock %}
