<form method="post" action="{{ path('app_backoffice_categories_delete', {'id': category.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir désactiver cette catégorie?');" class="d-flex justify-content-end align-items-center">
    <input type="hidden" name="_token" value="{{ csrf_token('désactiver' ~ category.id) }}">
    <button class="btn btn-danger" style="margin-right: 150px;">Désactiver</button>
</form>
