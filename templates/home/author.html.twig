{% extends 'base.html.twig' %}

{% block title %}Accueil | Eco Friendly{% endblock %}

{% block body %}
    
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>Accueil auteur</h1>
    </div>
    
    <div class="container-fluid">
        <div class="row">

            <!-- Left Sidebar -->
            {% include "_fragments/_user_sidebar.html.twig" with {user:app.user} %}
    
            <!-- Main Content -->
            <div class="col-md-9">
                {% if (articles is defined) and (articles is not empty) %}
                <div class="row">
                    {% for article in articles %}
                    <div class="col-md-4 mb-4">
                        <div class="card">
                            <img src="https://picsum.photos/300/200" class="card-img-top" alt="..." />
                            <div class="card-body">
                                <h5 class="card-title">{{ article.title }}</h5>
                                <div class="d-flex justify-content-between align-items-center">
                                    <h6 class="card-subtitle mb-2 text-muted">Crée le {{ article.createdAt|date('d/m/Y') }}</h6>
                                    <p class="float-end">{% include "_fragments/_content_status_tag.html.twig" with {status:article.status} %}</p>
                                </div>
                                <p class="card-text">{{ article.content|slice(0, 100) }} [...]</p>
                                <div class="d-flex flex-nowrap justify-content-end align-items-center">
                                    <a class="m-1 btn-sm btn btn-success" data-bs-toggle="tooltip" data-bs-placement="top" title="Voir" href="{{ path('app_backoffice_articles_show', {'id': article.id}) }}"><i class="bi bi-eye"></i></a>
                                    <a class="m-1 btn-sm btn btn-warning" data-bs-toggle="tooltip" data-bs-placement="top" title="&Eacute;diter" href="{{ path('app_backoffice_articles_edit', {'id': article.id}) }}"><i class="bi bi-pencil"></i></a>
                                    <form method="post" action="{{ path('app_backoffice_articles_deactivate', {'id': article.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir désactiver cet article ?');">
                                        <input type="hidden" name="_token" value="{{ csrf_token('deactivate' ~ article.id) }}" />
                                        <button class="m-1 btn-sm btn btn-danger" data-bs-toggle="tooltip" data-bs-placement="top" title="D&eacute;sactiver"><i class="bi bi-x-circle"></i></button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    </div>
                    {% endfor %}
                </div>
                {% endif %}
                {% if (advices is defined) and (advices is not empty) %}
                    <div class="row">
                        {% for advice in advices %}
                        <div class="col-md-4 mb-4">
                            <div class="card">
                                <img src="https://picsum.photos/300/200" class="card-img-top" alt="..." />
                                <div class="card-body">
                                    <h5 class="card-title">{{ advice.title }}</h5>
                                    <div class="d-flex justify-content-between align-items-center">
                                        <h6 class="card-subtitle mb-2 text-muted">Crée le {{ advice.createdAt|date('d/m/Y') }}</h6>
                                        <p class="float-end">{% include "_fragments/_content_status_tag.html.twig" with {status:advice.status} %}</p>
                                    </div>
                                    <p class="card-text">{{ advice.content|slice(0, 100) }} [...]</p>
                                    <div class="d-flex flex-nowrap justify-content-end align-items-center">
                                        <a class="m-1 btn-sm btn btn-success" data-bs-toggle="tooltip" data-bs-placement="top" title="Voir" href="{{ path('app_backoffice_advices_show', {'id': advice.id}) }}"><i class="bi bi-eye"></i></a>
                                        <a class="m-1 btn-sm btn btn-warning" data-bs-toggle="tooltip" data-bs-placement="top" title="&Eacute;diter" href="{{ path('app_backoffice_advices_edit', {'id': advice.id}) }}"><i class="bi bi-pencil"></i></a>
                                        <form method="post" action="{{ path('app_backoffice_advices_deactivate', {'id': advice.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir désactiver cet advice ?');">
                                            <input type="hidden" name="_token" value="{{ csrf_token('deactivate' ~ advice.id) }}" />
                                            <button class="m-1 btn-sm btn btn-danger" data-bs-toggle="tooltip" data-bs-placement="top" title="D&eacute;sactiver"><i class="bi bi-x-circle"></i></button>
                                        </form>
                                    </div>
                                </div>
                            </div>
                        </div>
                        {% endfor %}
                    </div>
                    {% endif %}
            </div>
        </div>
    </div>

    {% endblock %}